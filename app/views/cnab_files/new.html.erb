<main class="upload-container">
  <div class="upload-card">
    <h1>Upload de Arquivo CNAB</h1>
    <p class="subtitle">Selecione o arquivo CNAB para importar as transações</p>

    <%= form_with model: @cnab_file, url: cnab_files_path, multipart: true, local: true, class: "upload-form" do |form| %>
      <% if @cnab_file.errors.any? %>
        <div class="error-messages">
          <ul>
            <% @cnab_file.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="file-input-wrapper">
        <label for="cnab_file_file" class="file-label" style="position: relative; width: 100%;">
          <%= form.file_field :file, 
              accept: ".txt", 
              class: "file-input",
              id: "cnab_file_file",
              style: "position: absolute; inset: 0; width: 100%; height: 100%; opacity: 0; cursor: pointer; z-index: 2;" %>
          <span class="file-icon">📄</span>
          <span class="file-text">Clique para selecionar ou arraste o arquivo aqui</span>
          <span class="file-hint">Apenas arquivos .txt</span>
        </label>
      </div>

      <div class="form-actions">
        <%= form.submit "Enviar Arquivo", class: "submit-button" %>
      </div>
    <% end %>
  </div>
</main>